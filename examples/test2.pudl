extern printd(x);

def binary : 1 (x y) y;

# Iterative fib.
def fibi(x)
   var int a = 1, b = 1, c = 0 in
   (for i = 3, i < x in
      c = a + b :
      a = b :
      b = c) :
   b;

# Call it.
printd(fibi(20));